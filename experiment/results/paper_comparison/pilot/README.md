# Archived pilot

`shot50_seed123.json` and its prediction file are an early pilot retained for
auditability. It used a different seed and an exploratory refit procedure, so
it is not part of `paper_matched_summary.csv`, Figure 6, or any report claim.
Use the parent directory's three predeclared seeds (100/101/102) for the
paper-matched comparison.
